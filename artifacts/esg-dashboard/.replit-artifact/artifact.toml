kind = "web"
previewPath = "/"
title = "Breathe ESG"
version = "1.0.0"
id = "artifacts/esg-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18682

[services.development]
run = "pnpm --filter @workspace/esg-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/esg-dashboard", "run", "build" ]
publicDir = "artifacts/esg-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18682"
BASE_PATH = "/"
