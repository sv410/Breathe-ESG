services:
  # Django API (backend)
  - type: web
    name: breathe-esg-api
    runtime: python
    plan: free
    buildCommand: pip install -r artifacts/api-server/requirements.txt
    startCommand: >
      python artifacts/api-server/manage.py migrate --run-syncdb &&
      python artifacts/api-server/manage.py seed_users &&
      python artifacts/api-server/manage.py seed_demo_data &&
      gunicorn --chdir artifacts/api-server breathe_backend.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      # Optional (recommended for persistence): if you set this, Django will use Postgres.
      - key: DATABASE_URL
        sync: false


